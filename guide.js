someValues.forEach((element, index) => {
    console.log(`Current index: ${index}`);
    console.log(element);
});

//for each element under someValues object , arrow will give the output
.head{
    background-color: black;
    height : 0.5 px;
    width : 60px;
}

.snake{
    background-color: purple;
}

.food{
    background-color: brown;
    height : 15px;
    width: 15px;
}